# Scholr Metrics Dashboard

This folder contains recruiter-facing metrics assets.

- `dashboard-example.png` is a static dashboard preview for README and portfolio review.
- `dashboard-data.json` records the values used to generate the image.

Live proof values are used where available. Usage and feedback values in the image are sample-labelled placeholders until real analytics and student testing accumulate.
